# Frontend (React + Vite)

## Start

```bash
cd frontend
npm install
npm run dev
```

Frontend runs on:

`http://localhost:3000`

By default, Vite proxies `/api` and `/healthz` to:

`http://localhost:8000`

So start backend in parallel.

## Build

```bash
npm run build
npm run preview
```
